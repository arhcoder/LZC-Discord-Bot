const fetch = require("node-fetch");

module.exports.run = async (userID) =>
{
    console.log("Existe?");
    // const response = await fetch("https://localhost:3000/exists/"+userID);
    // console.log(response);
};